/**
 * Dashboard Feature Types
 *
 * Re-exports from shared types for backward compatibility
 */

export type { Account, MonthlySnapshot, AssetCategory } from '@/shared/types';
export { ASSET_CLASS_CATEGORIES, getAccountCategory } from '@/shared/types';
